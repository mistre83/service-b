name: PR Checks

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - 'CHANGELOG.md'
      - 'version.txt'

jobs:
  pr-checks:
    uses: mistre83/shared-workflows/.github/workflows/pr-checks.yml@main
    secrets: inherit
